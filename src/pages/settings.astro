---
import AppLayout from "../layouts/AppLayout.astro";
import { SettingsViewWrapper } from "../components/settings/SettingsViewWrapper";
import { requireAuth } from "../lib/utils/auth";

// Ensure user is authenticated
const user = requireAuth(Astro);
---

<AppLayout title="Ustawienia - DailyPath" user={user}>
  <div class="space-y-6">
    <div>
      <h1 class="text-3xl font-bold tracking-tight">Ustawienia</h1>
      <p class="text-muted-foreground">Zarządzaj swoim kontem i preferencjami</p>
    </div>

    <SettingsViewWrapper client:load initialUser={user} />
  </div>
</AppLayout>

